openapi: 3.0.3
info:
  title: Amar Pathagar API
  description: |
    Trust-Based Community Library System API
    
    A comprehensive API for managing a community-driven book sharing platform where trust and reputation matter.
  version: 1.0.0
  contact:
    name: NesoHQ
    url: https://github.com/nesohq
  license:
    name: MIT

servers:
  - url: http://localhost:8080/api/v1
    description: Development server
  - url: https://api.amarpathagar.com/api/v1
    description: Production server

tags:
  - name: Authentication
    description: User authentication and registration
  - name: Users
    description: User profile and management
  - name: Books
    description: Book catalog and management
  - name: Book Requests
    description: Book request and borrowing
  - name: Handover
    description: Book handover and delivery coordination
  - name: Ideas
    description: Book suggestions and ideas
  - name: Reviews
    description: Book reviews and ratings
  - name: Bookmarks
    description: User bookmarks and favorites
  - name: Donations
    description: Book donations
  - name: Notifications
    description: User notifications
  - name: Admin
    description: Administrative operations (admin only)
  - name: Leaderboard
    description: User rankings and statistics

components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  schemas:
    User:
      type: object
      properties:
        id:
          type: string
          format: uuid
        username:
          type: string
        email:
          type: string
          format: email
        full_name:
          type: string
        role:
          type: string
          enum: [admin, member]
        avatar_url:
          type: string
        bio:
          type: string
        location_lat:
          type: number
          format: double
        location_lng:
          type: number
          format: double
        location_address:
          type: string
        success_score:
          type: integer
        books_shared:
          type: integer
        books_received:
          type: integer
        total_upvotes:
          type: integer
        total_downvotes:
          type: integer
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    Book:
      type: object
      properties:
        id:
          type: string
          format: uuid
        title:
          type: string
        author:
          type: string
        isbn:
          type: string
        cover_url:
          type: string
        description:
          type: string
        category:
          type: string
        tags:
          type: array
          items:
            type: string
        physical_code:
          type: string
        status:
          type: string
          enum: [available, reading, reserved, requested, on_hold]
        max_reading_days:
          type: integer
        current_holder_id:
          type: string
          format: uuid
        current_holder:
          $ref: '#/components/schemas/User'
        created_by:
          type: string
          format: uuid
        total_reads:
          type: integer
        average_rating:
          type: number
          format: float
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    BookRequest:
      type: object
      properties:
        id:
          type: string
          format: uuid
        book_id:
          type: string
          format: uuid
        book:
          $ref: '#/components/schemas/Book'
        user_id:
          type: string
          format: uuid
        user:
          $ref: '#/components/schemas/User'
        status:
          type: string
          enum: [pending, approved, rejected]
        priority_score:
          type: number
          format: float
        requested_at:
          type: string
          format: date-time
        processed_at:
          type: string
          format: date-time
        due_date:
          type: string
          format: date-time

    HandoverThread:
      type: object
      properties:
        id:
          type: string
          format: uuid
        book_id:
          type: string
          format: uuid
        book:
          $ref: '#/components/schemas/Book'
        current_holder_id:
          type: string
          format: uuid
        current_holder:
          $ref: '#/components/schemas/User'
        next_holder_id:
          type: string
          format: uuid
        next_holder:
          $ref: '#/components/schemas/User'
        status:
          type: string
          enum: [active, completed, cancelled]
        handover_due_date:
          type: string
          format: date-time
        is_public:
          type: boolean
        created_at:
          type: string
          format: date-time
        completed_at:
          type: string
          format: date-time

    HandoverMessage:
      type: object
      properties:
        id:
          type: string
          format: uuid
        thread_id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        user:
          $ref: '#/components/schemas/User'
        message:
          type: string
        is_system_message:
          type: boolean
        created_at:
          type: string
          format: date-time

    Idea:
      type: object
      properties:
        id:
          type: string
          format: uuid
        book_id:
          type: string
          format: uuid
        book:
          $ref: '#/components/schemas/Book'
        user_id:
          type: string
          format: uuid
        user:
          $ref: '#/components/schemas/User'
        title:
          type: string
        content:
          type: string
        upvotes:
          type: integer
        downvotes:
          type: integer
        created_at:
          type: string
          format: date-time

    UserReview:
      type: object
      properties:
        id:
          type: string
          format: uuid
        reviewer_id:
          type: string
          format: uuid
        reviewer:
          $ref: '#/components/schemas/User'
        reviewee_id:
          type: string
          format: uuid
        reviewee:
          $ref: '#/components/schemas/User'
        book_id:
          type: string
          format: uuid
        behavior_rating:
          type: integer
          minimum: 1
          maximum: 5
        book_condition_rating:
          type: integer
          minimum: 1
          maximum: 5
        communication_rating:
          type: integer
          minimum: 1
          maximum: 5
        comment:
          type: string
        created_at:
          type: string
          format: date-time

    Notification:
      type: object
      properties:
        id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        type:
          type: string
        title:
          type: string
        message:
          type: string
        is_read:
          type: boolean
        created_at:
          type: string
          format: date-time

    Donation:
      type: object
      properties:
        id:
          type: string
          format: uuid
        donor_id:
          type: string
          format: uuid
        donor:
          $ref: '#/components/schemas/User'
        amount:
          type: number
          format: float
        message:
          type: string
        created_at:
          type: string
          format: date-time

    ReadingHistory:
      type: object
      properties:
        id:
          type: string
          format: uuid
        book_id:
          type: string
          format: uuid
        book:
          $ref: '#/components/schemas/Book'
        reader_id:
          type: string
          format: uuid
        reader:
          $ref: '#/components/schemas/User'
        start_date:
          type: string
          format: date-time
        end_date:
          type: string
          format: date-time
        duration_days:
          type: integer
        created_at:
          type: string
          format: date-time

    Error:
      type: object
      properties:
        error:
          type: string
        message:
          type: string

    Success:
      type: object
      properties:
        data:
          type: object
        message:
          type: string

paths:
  /health:
    get:
      summary: Health check
      description: Check if the API is running
      tags:
        - Health
      responses:
        '200':
          description: API is healthy
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok

  /auth/register:
    post:
      summary: Register new user
      description: Create a new user account
      tags:
        - Authentication
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - username
                - email
                - password
                - full_name
              properties:
                username:
                  type: string
                  example: johndoe
                email:
                  type: string
                  format: email
                  example: john@example.com
                password:
                  type: string
                  format: password
                  example: SecurePass123!
                full_name:
                  type: string
                  example: John Doe
      responses:
        '201':
          description: User registered successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      user:
                        $ref: '#/components/schemas/User'
                      access_token:
                        type: string
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /auth/login:
    post:
      summary: User login
      description: Authenticate user and get access token
      tags:
        - Authentication
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - username
                - password
              properties:
                username:
                  type: string
                  example: johndoe
                password:
                  type: string
                  format: password
                  example: SecurePass123!
      responses:
        '200':
          description: Login successful
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      user:
                        $ref: '#/components/schemas/User'
                      access_token:
                        type: string
        '401':
          description: Invalid credentials
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /me:
    get:
      summary: Get current user
      description: Get authenticated user information
      tags:
        - Authentication
      security:
        - BearerAuth: []
      responses:
        '200':
          description: Current user information
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      user_id:
                        type: string
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /users/{id}/profile:
    get:
      summary: Get user profile
      description: Get public profile of a user
      tags:
        - Users
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: User profile
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/User'
        '404':
          description: User not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /users/profile:
    put:
      summary: Update user profile
      description: Update authenticated user's profile
      tags:
        - Users
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                full_name:
                  type: string
                bio:
                  type: string
                avatar_url:
                  type: string
                location_address:
                  type: string
                location_lat:
                  type: number
                location_lng:
                  type: number
      responses:
        '200':
          description: Profile updated successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/User'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /users/interests:
    post:
      summary: Add user interests
      description: Add reading interests for the user
      tags:
        - Users
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - interests
              properties:
                interests:
                  type: array
                  items:
                    type: string
                  example: ["Science Fiction", "History", "Philosophy"]
      responses:
        '200':
          description: Interests added successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'

  /leaderboard:
    get:
      summary: Get leaderboard
      description: Get top users by success score
      tags:
        - Leaderboard
      security:
        - BearerAuth: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 10
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        '200':
          description: Leaderboard data
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/User'

  /books:
    get:
      summary: List all books
      description: Get paginated list of books with optional filters
      tags:
        - Books
      security:
        - BearerAuth: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
        - name: search
          in: query
          schema:
            type: string
        - name: category
          in: query
          schema:
            type: string
        - name: status
          in: query
          schema:
            type: string
            enum: [available, reading, reserved, requested, on_hold]
      responses:
        '200':
          description: List of books
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Book'
    post:
      summary: Create new book
      description: Add a new book to the library (admin only)
      tags:
        - Books
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - title
                - author
                - physical_code
              properties:
                title:
                  type: string
                author:
                  type: string
                isbn:
                  type: string
                description:
                  type: string
                category:
                  type: string
                physical_code:
                  type: string
                max_reading_days:
                  type: integer
                  default: 14
      responses:
        '201':
          description: Book created successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/Book'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /books/{id}:
    get:
      summary: Get book by ID
      description: Get detailed information about a specific book
      tags:
        - Books
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Book details
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/Book'
        '404':
          description: Book not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    patch:
      summary: Update book
      description: Update book information
      tags:
        - Books
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                title:
                  type: string
                author:
                  type: string
                isbn:
                  type: string
                description:
                  type: string
                category:
                  type: string
                max_reading_days:
                  type: integer
      responses:
        '200':
          description: Book updated successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/Book'
        '404':
          description: Book not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

    delete:
      summary: Delete book
      description: Remove a book from the library
      tags:
        - Books
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Book deleted successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'
        '404':
          description: Book not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /books/{id}/request:
    post:
      summary: Request a book
      description: Request to borrow a book
      tags:
        - Book Requests
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '201':
          description: Book requested successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/BookRequest'
        '400':
          description: Bad request (e.g., book not available, insufficient score)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

    delete:
      summary: Cancel book request
      description: Cancel a pending book request
      tags:
        - Book Requests
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Request cancelled successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'
        '404':
          description: Request not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /books/{id}/requested:
    get:
      summary: Check if book is requested
      description: Check if the current user has requested this book
      tags:
        - Book Requests
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Request status
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      requested:
                        type: boolean

  /books/{id}/return:
    post:
      summary: Return a book
      description: Return a borrowed book
      tags:
        - Books
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Book returned successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /books/{id}/complete:
    post:
      summary: Mark book as completed
      description: Mark that you have finished reading the book
      tags:
        - Handover
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Book marked as completed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /books/{id}/delivered:
    post:
      summary: Mark book as delivered
      description: Confirm that you have received the book
      tags:
        - Handover
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Book marked as delivered
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /books/{id}/handover:
    get:
      summary: Get active handover thread
      description: Get the active handover thread for a book
      tags:
        - Handover
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Handover thread details
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/HandoverThread'
        '404':
          description: No active handover thread found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /books/{id}/reading-status:
    get:
      summary: Get reading status
      description: Get extended reading history for current holder
      tags:
        - Handover
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Reading status
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/ReadingHistory'
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /my-requests:
    get:
      summary: Get user's book requests
      description: Get all book requests made by the current user
      tags:
        - Book Requests
      security:
        - BearerAuth: []
      responses:
        '200':
          description: List of user's book requests
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/BookRequest'

  /my-reading-history:
    get:
      summary: Get reading history
      description: Get the current user's reading history
      tags:
        - Books
      security:
        - BearerAuth: []
      responses:
        '200':
          description: Reading history
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/ReadingHistory'

  /my-books-on-hold:
    get:
      summary: Get books on hold
      description: Get books that the current user is holding (status on_hold)
      tags:
        - Books
      security:
        - BearerAuth: []
      responses:
        '200':
          description: List of books on hold
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Book'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /handover/threads:
    get:
      summary: Get user's handover threads
      description: Get all handover threads for the current user
      tags:
        - Handover
      security:
        - BearerAuth: []
      responses:
        '200':
          description: List of handover threads
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/HandoverThread'

  /handover/threads/{id}/messages:
    get:
      summary: Get handover messages
      description: Get all messages in a handover thread
      tags:
        - Handover
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: List of messages
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/HandoverMessage'

    post:
      summary: Post handover message
      description: Post a message to a handover thread
      tags:
        - Handover
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - message
              properties:
                message:
                  type: string
      responses:
        '201':
          description: Message posted successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /ideas:
    post:
      summary: Create book idea
      description: Suggest a new book idea or discussion topic
      tags:
        - Ideas
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - book_id
                - title
                - content
              properties:
                book_id:
                  type: string
                  format: uuid
                title:
                  type: string
                content:
                  type: string
      responses:
        '201':
          description: Idea created successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/Idea'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /ideas/book/{bookId}:
    get:
      summary: Get ideas for a book
      description: Get all ideas/discussions for a specific book
      tags:
        - Ideas
      security:
        - BearerAuth: []
      parameters:
        - name: bookId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: List of ideas
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Idea'

  /ideas/{id}/vote:
    post:
      summary: Vote on an idea
      description: Upvote or downvote an idea
      tags:
        - Ideas
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - vote_type
              properties:
                vote_type:
                  type: string
                  enum: [upvote, downvote]
      responses:
        '200':
          description: Vote recorded successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /reviews:
    post:
      summary: Create user review
      description: Write a review for another user based on book exchange experience
      tags:
        - Reviews
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - reviewee_id
              properties:
                reviewee_id:
                  type: string
                  format: uuid
                  description: ID of the user being reviewed
                book_id:
                  type: string
                  format: uuid
                  description: ID of the book related to this review
                behavior_rating:
                  type: integer
                  minimum: 1
                  maximum: 5
                  description: Rating for user behavior
                book_condition_rating:
                  type: integer
                  minimum: 1
                  maximum: 5
                  description: Rating for book condition
                communication_rating:
                  type: integer
                  minimum: 1
                  maximum: 5
                  description: Rating for communication
                comment:
                  type: string
                  description: Optional review comment
      responses:
        '201':
          description: Review created successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/UserReview'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /users/{id}/reviews:
    get:
      summary: Get user reviews
      description: Get all reviews received by a user
      tags:
        - Reviews
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: List of reviews
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/UserReview'

  /books/{id}/reviews:
    get:
      summary: Get book reviews
      description: Get all reviews related to a specific book
      tags:
        - Reviews
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: List of reviews for the book
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/UserReview'

  /bookmarks:
    get:
      summary: Get user bookmarks
      description: Get all bookmarks for the current user
      tags:
        - Bookmarks
      security:
        - BearerAuth: []
      responses:
        '200':
          description: List of bookmarks
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                          format: uuid
                        book_id:
                          type: string
                          format: uuid
                        book:
                          $ref: '#/components/schemas/Book'
                        bookmark_type:
                          type: string
                        created_at:
                          type: string
                          format: date-time
    post:
      summary: Create bookmark
      description: Bookmark a book
      tags:
        - Bookmarks
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - book_id
                - bookmark_type
              properties:
                book_id:
                  type: string
                  format: uuid
                bookmark_type:
                  type: string
                  enum: [want_to_read, reading, completed]
      responses:
        '201':
          description: Bookmark created successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /bookmarks/{bookId}:
    delete:
      summary: Delete bookmark
      description: Remove a bookmark
      tags:
        - Bookmarks
      security:
        - BearerAuth: []
      parameters:
        - name: bookId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Bookmark deleted successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'
        '404':
          description: Bookmark not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /donations:
    get:
      summary: List donations
      description: Get all donations
      tags:
        - Donations
      security:
        - BearerAuth: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        '200':
          description: List of donations
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Donation'
    post:
      summary: Create donation
      description: Make a donation to the library
      tags:
        - Donations
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - amount
              properties:
                amount:
                  type: number
                  format: float
                message:
                  type: string
      responses:
        '201':
          description: Donation created successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/Donation'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /notifications:
    get:
      summary: Get user notifications
      description: Get all notifications for the current user
      tags:
        - Notifications
      security:
        - BearerAuth: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        '200':
          description: List of notifications
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Notification'

  /notifications/{id}/read:
    put:
      summary: Mark notification as read
      description: Mark a specific notification as read
      tags:
        - Notifications
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Notification marked as read
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'
        '404':
          description: Notification not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /notifications/read-all:
    put:
      summary: Mark all notifications as read
      description: Mark all user notifications as read
      tags:
        - Notifications
      security:
        - BearerAuth: []
      responses:
        '200':
          description: All notifications marked as read
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'

  /admin/stats:
    get:
      summary: Get system statistics
      description: Get overall system statistics (admin only)
      tags:
        - Admin
      security:
        - BearerAuth: []
      responses:
        '200':
          description: System statistics
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      total_users:
                        type: integer
                      total_books:
                        type: integer
                      available_books:
                        type: integer
                      books_in_circulation:
                        type: integer
                      pending_requests:
                        type: integer
                      total_donations:
                        type: integer
                      total_ideas:
                        type: integer
                      total_reviews:
                        type: integer
                      avg_success_score:
                        type: number
                        format: float
        '403':
          description: Forbidden - Admin access required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /admin/audit-logs:
    get:
      summary: Get audit logs
      description: Get system audit logs (admin only)
      tags:
        - Admin
      security:
        - BearerAuth: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 50
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        '200':
          description: List of audit logs
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                          format: uuid
                        user_id:
                          type: string
                          format: uuid
                        action:
                          type: string
                        resource_type:
                          type: string
                        resource_id:
                          type: string
                        details:
                          type: object
                        ip_address:
                          type: string
                        user_agent:
                          type: string
                        created_at:
                          type: string
                          format: date-time

  /admin/requests/pending:
    get:
      summary: Get pending book requests
      description: Get all pending book requests (admin only)
      tags:
        - Admin
      security:
        - BearerAuth: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 50
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        '200':
          description: List of pending requests
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/BookRequest'

  /admin/requests/{id}/approve:
    post:
      summary: Approve book request
      description: Approve a pending book request (admin only)
      tags:
        - Admin
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - due_date
              properties:
                due_date:
                  type: string
                  format: date-time
                  example: "2026-02-15T00:00:00Z"
      responses:
        '200':
          description: Request approved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /admin/requests/{id}/reject:
    post:
      summary: Reject book request
      description: Reject a pending book request (admin only)
      tags:
        - Admin
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - reason
              properties:
                reason:
                  type: string
      responses:
        '200':
          description: Request rejected successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /admin/books/{bookId}/requests:
    get:
      summary: Get requests for a book
      description: Get all requests for a specific book (admin only)
      tags:
        - Admin
      security:
        - BearerAuth: []
      parameters:
        - name: bookId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: List of requests for the book
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/BookRequest'

  /admin/users:
    get:
      summary: Get all users
      description: Get list of all users (admin only)
      tags:
        - Admin
      security:
        - BearerAuth: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 50
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        '200':
          description: List of users
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/User'

  /admin/users/{userId}/score:
    post:
      summary: Adjust user success score
      description: Manually adjust a user's success score (admin only)
      tags:
        - Admin
      security:
        - BearerAuth: []
      parameters:
        - name: userId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - amount
                - reason
              properties:
                amount:
                  type: integer
                  description: Positive or negative amount to adjust
                  example: 10
                reason:
                  type: string
                  example: "Exceptional community contribution"
      responses:
        '200':
          description: Score adjusted successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /admin/users/{userId}/role:
    put:
      summary: Update user role
      description: Change a user's role (admin only)
      tags:
        - Admin
      security:
        - BearerAuth: []
      parameters:
        - name: userId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - role
              properties:
                role:
                  type: string
                  enum: [admin, member]
      responses:
        '200':
          description: Role updated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /admin/books:
    get:
      summary: Get all books (admin view)
      description: Get all books with admin filters (admin only)
      tags:
        - Admin
      security:
        - BearerAuth: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 50
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
        - name: search
          in: query
          schema:
            type: string
        - name: category
          in: query
          schema:
            type: string
        - name: status
          in: query
          schema:
            type: string
      responses:
        '200':
          description: List of books
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Book'

  /admin/books/{bookId}/status:
    put:
      summary: Update book status
      description: Manually update a book's status (admin only)
      tags:
        - Admin
      security:
        - BearerAuth: []
      parameters:
        - name: bookId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - status
              properties:
                status:
                  type: string
                  enum: [available, reading, reserved, requested, on_hold]
      responses:
        '200':
          description: Status updated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
